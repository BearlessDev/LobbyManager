main: ${project.groupId}.${project.name}

name: ${project.name}
version: ${project.version}
author: Bearless_
softdepend: [ LuckPerms ]

commands:
  lobbymanager-spawn:
    aliases: [lm-spawn, spawn]
  lobbymanager-setspawn:
    aliases: [lm-setspawn, setspawn]
  lobbymanager-reload:
    aliases: [lm-reload]